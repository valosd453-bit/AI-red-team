# ---------------------------------------------------------------------------
# Ai Red Toolkit + Agathon orchestrator — unified Python deps for Railway.
# Railpack auto-detects Python and runs `pip install -r requirements.txt`,
# so EVERYTHING the orchestrator needs at runtime must live in this file.
# ---------------------------------------------------------------------------

# --- Core toolkit (used by attacks/*.py + forgeguard_bridge.py) ------------
requests>=2.31,<3.0
httpx>=0.27,<0.29
openai>=1.12,<2.0
python-dotenv>=1.0,<2.0
tenacity>=8.2,<9.0
tqdm>=4.66,<5.0
beautifulsoup4>=4.12,<5.0
numpy>=1.26,<3.0
pillow>=10.0,<12.0
playwright>=1.44,<2.0
dnspython>=2.6,<3.0

# --- Agathon orchestrator (FastAPI worker on Railway) ----------------------
fastapi>=0.115,<0.116
uvicorn[standard]>=0.32,<0.33
pydantic>=2.7,<3.0
groq>=0.13,<2.0
supabase>=2.10,<3.0
websockets>=13.0,<14.0
aiohttp>=3.9,<4.0

# Global attack libraries (Garak probe catalogue — 450+ probes via garak_catalog)
garak>=0.9.0,<0.11.0

# Phase 2: PyRIT intent-drift / tool-call injection (adapter not wired in Phase 1)
# pyrit>=0.9.0
