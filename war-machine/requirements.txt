# ForgeGuard War Machine — Python Dependencies
# Install: pip install -r requirements.txt
# Then:    playwright install chromium

# Core
playwright==1.44.0          # Headless browser for scraping
supabase==2.4.6             # Supabase Python client (service role)
httpx==0.27.0               # Async HTTP — OpenRouter + Resend API calls
python-dotenv==1.0.1        # .env file loading

# Utilities
tenacity==8.3.0             # Retry logic with exponential backoff (optional)
